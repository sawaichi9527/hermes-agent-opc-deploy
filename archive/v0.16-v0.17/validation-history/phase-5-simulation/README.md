# Phase 5 simulation validation archive

Historical repository-local simulation validation material from the earlier implementation route.

This group is no longer part of the active mainline. It remains useful only as background safety evidence.

Former mainline documents and scripts in this group included:

```text
docs/phase-5-simulation-trial.md
scripts/prepare-sim-env.sh
scripts/deploy-sim-profiles.sh
scripts/inspect-sim-profiles.sh
scripts/verify-sim-layout.sh
```

Current status:

```text
historical / not required by verify-repo-layout.sh / not the active implementation route
```

The current mainline no longer treats repository-local simulation deployment as the default route. Future real use should align with Hermes Agent native profile behavior and explicitly approved profile changes only.
